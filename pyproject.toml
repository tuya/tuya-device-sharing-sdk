[tool.ruff.lint]
ignore = [
    "E712",    # Avoid equality comparisons to `True`
    "F401",    # Variable is shadowing a Python builtin
    "F541",    # f-string without any placeholders
    "F841",    # Local variable is assigned to but never used
]